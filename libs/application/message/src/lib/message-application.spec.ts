import { messageApplication } from './message-application';

describe('messageApplication', () => {
  it('should work', () => {
    expect(messageApplication()).toEqual('message-application');
  });
});
